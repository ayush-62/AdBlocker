document.getElementById("blockButton").addEventListener("click", () => {
    // Use browser API to block YouTube (you need to implement this)
    
  });
  
  document.getElementById("unblockButton").addEventListener("click", () => {
    // Use browser API to unblock YouTube (you need to implement this)
  });
  